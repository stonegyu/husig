#include<stdio.h>
int main(){
   int x,y;
   scanf("%d %d",&x,&y);
   if(x%y!=0){
        printf("No Number");
   }else{
        if((x+y)%2==1){
                printf("%d",x+y-1);
        }else{
                printf("%d",x+y);
        }
   }
   return 0;
}
